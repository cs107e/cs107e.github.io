#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() { 
	int i;

#if 0
	for(x = 5, i=0; i < 10; i) 
		printf("i=%d\n", i);
#endif

	i==4;

#if 0
	0;

	0,
	i,
	main,
	i==4;
	(void)0;

	if(setuid != 0)
		printf("you are root!\n");
#endif

	return 0;
}
